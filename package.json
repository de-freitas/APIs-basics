{
  "name": "node.js-first-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx ts-node server.ts",
    "services:up": "docker compose --file infra/compose.yaml up --detach",
    "services:down": "docker compose --file infra/compose.yaml down",
    "migrations:create": "npx drizzle-kit generate",
    "migrations:up": "npx drizzle-kit migrate",
    "first-start-all": "npm run services:up && npm run migrations:up && npm run dev && npx drizzle-kit studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "fastify": "^5.6.1",
    "fastify-type-provider-zod": "^6.1.0",
    "pg": "^8.16.3",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "drizzle-kit": "^0.31.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
